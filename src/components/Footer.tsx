export default function Footer() {
  return (
    <footer className="footer footer-center p-10 bg-base-100 text-base-content mt-12">
      <aside>
        <p className="font-semibold">StrCounter</p>
        <p>Built with React, Tailwind CSS, and daisyUI</p>
      </aside>
    </footer>
  )
}
